# Logic to load and parse student submissions

def load_submissions(directory_path):
    # Read all submission files from the specified directory
    # Parse each submission into a usable format
    # Return a list of parsed submissions
