# Configuration settings

# Load configuration settings (e.g., LLM provider, API keys)
# Return the configuration settings
def load_config():
    # Placeholder for loading configuration settings
    config = {
        'llm_provider': 'openai',
        'rubric_path': 'path/to/rubric.csv'
    }
    return config