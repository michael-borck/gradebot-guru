# Handles LLM API communication

def get_response(prompt):
    # 1. Send request to LLM API (e.g., OpenAI)
    # 2. Handle errors (e.g., rate limits, API failures)
    # 3. Return LLM response
    pass

def parse_response(response):
    # 1. Extract grade and feedback from LLM response
    # 2. Handle potential inconsistencies or errors in response
    return grade, feedback
