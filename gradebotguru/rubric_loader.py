# Logic to load and parse rubrics

# Read the rubric file (CSV, JSON)
# Parse the file content into a usable format
# Return the parsed rubric

def load_rubric(file_path):
    # Placeholder for loading rubric from file
    rubric = {
        'criteria': 'example criteria',
        'weight': 1.0
    }
    return rubric
